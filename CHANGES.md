# 修复与变更记录 — 2026-05-12

## 背景

在 2026-05-11 的全面代码审计中，对项目代码、论文原文、以及复现工作报告进行了三方交叉核对，发现若干不一致和缺失项。本次会话完成了全部修复。

## 变更清单

### 1. 高度网格：88 层 → 93 层（核心修复）

**文件**：`config.py`

**问题**：原代码的 `build_height_grid()` 仅生成 88 层，而论文 §3.1.3 和 §3.3.2 明确要求 93 层。代码注释中写的 "93 layers" 与实际运行结果矛盾。

**修复**：重写 `build_height_grid()`，使用 `np.linspace` 在 8 个高度段内均匀分布，共 93 层，段边界对齐论文 Table 2.1 的整数值（0, 500, 1000, 1500, 2000, 3000, 5000, 6000, 10000m）。

实际间距与 Table 2.1 名义值对比：

| 段 | 名义分辨率 | 实际间距 | 层数 |
|----|----------|---------|------|
| 0-500m | 30m | 27.8m | 19 |
| 500-1000m | 40m | 35.7m | 15 |
| 1000-1500m | 60m | 50.0m | 11 |
| 1500-2000m | 90m | 71.4m | 8 |
| 2000-3000m | 120m | 100.0m | 11 |
| 3000-5000m | 160m | 153.8m | 14 |
| 5000-6000m | 200m | 166.7m | 7 |
| 6000-10000m | 250m | 285.7m | 15 |

**待验证**：对照论文 Fig 3.10 截图确认每段层数与图中一致。若不同，只需调整 `segments` 列表中各段的 `n` 参数。

**影响范围**：
- `config.N_LAYERS` 自动变为 93，所有引用该变量的代码无需修改
- 6 个 BRNN 子模型的输出维度随之更新：T/RH_0-2km 输出 50 节点，2-8km 输出 37 节点，8-10km 输出 8 节点
- 边界层（2km, 8km）在相邻模型中各被预测一次，总输出 95 节点对应 93 个唯一高度层

### 2. MonoRTM TAPE3 搜索路径补充

**文件**：`src/monortm_wrapper.py`

**问题**：`_find_tape3()` 不包含 `data/TAPE3` 路径，而 `setup_monortm.sh` 正是将 TAPE3 安装到该路径。

**修复**：在候选路径列表顶部添加 `data/TAPE3`（相对于项目根目录解析）。

### 3. run.py 导入路径统一

**文件**：`run.py`

**问题**：第 317 行 `from evaluate import ...` 不带 `src.` 前缀，与文件中其他 6 处导入风格不一致。

**修复**：改为 `from src.evaluate import ...`。

### 4. 文档更新

**文件**：`README.md`
- 项目结构中新增 `download_era5.py`（CDS 备选下载脚本，带本地代理配置）
- 全部 "88层" 描述更正为 "93层"
- 高度网格表格更新为实际 93 层数据
- BRNN 模型配置表新增输出层数列

**文件**：`CHANGES.md`（新文件，即本文档）

## 审核确认清单

对照论文原文进行的代码审计结论（13 项与论文一致，无需修改）：

| # | 项目 | 论文出处 | 
|---|------|---------|
| 1 | 14 通道频率 | §2.1.2 |
| 2 | ERA5 37 层等压面 | §3.1.1 |
| 3 | 2m RH Magnus 公式(3-1~3-3) | §3.1.1 |
| 4 | LWC 经验估算 | §3.1.2 |
| 5 | MonoRTM IATM=0 (Case3/Case5) | §3.1.4 |
| 6 | QC Step1: RH×0.9 缩放 | §3.2.2 |
| 7 | QC Step2: 四季逐层偏差订正 | §3.2.2 |
| 8 | QC Step3: ICLWC>1250 删除 / 750-1250 缩放 | §3.2.2 |
| 9 | QC Step4: 14 通道线性 BT 订正 | §3.2.3 |
| 10 | BRNN 网络架构 | §3.3.1, Fig 3.9 |
| 11 | 6 子模型策略 | §3.3.2 |
| 12 | T_0-2km 输入 6 维 | §3.3.2, Fig 3.12 |
| 13 | 训练/验证年份 | §3.1.2 |

## 待办提醒（未变）

1. **ERA5 数据下载**（最优先）— CDS 账号问题
2. **TAPE3 光谱数据** — 运行 `setup_monortm.sh` 或手动下载
3. **MWR 实测亮温** — 联系论文作者
4. **真实数据训练** — 依赖 1-3

## 下次启动项目

```bash
cd /Users/ink/test/mwr_retrieval

# 先验证 93 层网格配置正确
python3 -c "import config; print(f'Layers: {config.N_LAYERS}')"

# 数据就绪后运行全流程
python run.py --stage all
```

---

# 会话记录 — 2026-05-21/22

## ERA5 数据下载攻坚战

### 环境修复

- **Python 升级**：系统 Python 从 3.13 升级到 3.14（brew upgrade 副作用），原 netCDF4/h5netcdf 失效
- **创建 venv**：`.venv/`，Python 3.14，安装全部依赖（netCDF4, h5netcdf, cdsapi, xarray, cfgrib, zarr, gcsfs, dask）
- **激活方式**：`source .venv/bin/activate`（必须！）

### CDS 探索（最终结论：不可用）

| 尝试 | key | 结果 |
|------|-----|------|
| 小请求测试 | 新 key `a2179a74` | accepted → rejected（队列限流） |
| 小请求测试 | 旧 key `e088c69b` | accepted → successful（33KB, 30s） |
| 完整月下载 | 旧 key | 成功（sl_2013_01, 529KB, 3min） |
| 后续月下载 | 旧 key | 403 Forbidden（用完配额？） |
| 切换新 key | 新 key `a2179a74` | 403 Forbidden（已死） |
| 两个 key | 全部 | **最终状态：均 403** |

**CDS 教训**：新 key 队列限流严重，旧 key 在 1 次成功后也 403。CDS 不适合批量下载。

### ARCO GCS 探索（最终结论：可用但需策略）

**成功验证：**
- ARCO = CDS 完全一致（温度差异 0.0000K，露点 0.0000K，气压 0.03 Pa）
- 单层 2013-01 下载成功：1.0 MB，3 分钟
- 气压层 2013-01 下载成功：12 MB（但温度/湿度可能有 fill 值问题）
- curl 直连 GCS 正常：单 chunk 1 秒下载

**关键问题：GCS 隐式限流**

| 现象 | 根因 |
|------|------|
| 第一个月 2013-01 成功（3 分钟） | ~2200 chunk 请求成功 |
| 第二个月 2013-02 超时 | GCS 对 IP 静默限流（非 HTTP 429） |

**尝试过的方案（均失败）：**
1. xarray 单进程循环 → 第二月卡住
2. 独立子进程每月 → 第二月卡住（IP 级别）
3. 30 秒延迟 → 第二月卡住
4. 显式 GC + 连接池清理 → 第二月卡住
5. 代理访问 GCS → ConnectionRefused（代理不支持 Google）

**待验证的最优方案：**
```bash
AIOHTTP_MAX_CONNECTIONS=2 python3 -c "..."
```
限制并发连接数从默认 100 降到 2，大幅降低请求速率。预估 ~18 分钟/月，84 月 × 18 分钟 = ~25 小时。

### 新增工具脚本

| 脚本 | 用途 |
|------|------|
| `dl_one_month.py` | 单月下载，独立进程，供 bash 循环调用 |
| `dl_sync.py` | subprocess 隔离，5 分钟超时强制 kill |
| `bulk_fast.py` | 30 秒间隔 xarray 会话级隔离 |
| `bulk_v3.py` | 显式清理连接池 + gc |
| `bulk_sl.py` | 单会话版（有连接复用问题） |
| `download_all.py` | CDS+ARCO 混合（CDS 已失效） |

### 气压层 specific_humidity 转换

ARCO 气压层提供 `specific_humidity` 而非 `relative_humidity`。需在预处理中转换（标准大气物理公式，利用同层温度+气压）。

### TAPE3 状态

- GitHub monoRTM 仓库不含 pre-built TAPE3 文件
- 需 Zenodo（aer_v3.8.1.tar.gz, 442MB），当前网络不可达
- `setup_monortm.sh` 脚本已就绪，网络恢复后运行即可

---

# 会话记录 — 2026-05-23

## 找到可靠下载策略：周级下载

### 关键发现

GCS 限流阈值在 72 ~ 2232 chunk 请求之间：
- 1 天下载（72 chunk）→ 连续运行 OK
- 1 周下载（504 chunk）→ 连续运行 OK
- 全月下载（2232 chunk）→ 首次 OK，后续超时

### 最终可靠方案

**`dl_weekly.py`** — 每 7 天下载一次，每月 4 周合并：

```python
for start_day in range(1, last_day+1, 7):
    ds = xr.open_zarr(ARCO, consolidated=True)
    chunk = ds[vars].sel(time=slice(...)).load()
    weeks.append(chunk)
    ds.close()
month_data = xr.concat(weeks, dim='time')
```

- 每次 ~504 chunk 请求，不触发 GCS 限流
- ~14 分钟/月，~1.0 MB/月
- 84 月 × 14 分 ≈ 20 小时

### 断点状态（2026-05-23）

| 项目 | 状态 |
|------|------|
| 单层 2013 | 12/12 完成 |
| 单层 2014 | 12/12 完成 |
| 单层 2015-2019 | 60 月待下载 |
| 气压层 | 仅 2013-01（12 MB，温度/湿度含 NaN） |
| 下载脚本 | `dl_weekly.py`（推荐继续使用） |

### 继续命令

```bash
cd /Users/ink/test/mwr_retrieval && source .venv/bin/activate
python3 -u dl_weekly.py  # 自动跳过 2013-2014，从 2015-01 开始
```

### 已废弃方案

- `AIOHTTP_MAX_CONNECTIONS=2` 环境变量 — 不确定是否生效
- fsspec 显式连接限制 — 参数透传 bug
- 子进程隔离 — 不能解决 IP 级别限流
- CDS API — 两个旧 key 均已 403

---

# 会话记录 — 2026-05-25/26/27

## 单层 ERA5 数据补齐（84/84 完成）

### 断点状态（2026-05-25）
- 54/84 月已完成，余 30 月

### 下载策略演变

**阶段1：周级 xarray/zarr（成功完成单层）**
- 使用 `dl_weekly.py`，每周 504 chunk 请求，不触发 GCS 限流
- 2018-11 至 2019-04 陆续补齐，每约 14-20 分钟
- GCS 限流逐渐加重，2019-03 起出现连接取消错误

**阶段2：带重试的周级下载**
- 添加 5 次重试 + 递增冷却（120s, 240s, 360s...）
- `AIOHTTP_MAX_CONNECTIONS=2` 环境变量限制并发
- 2019-05 至 2019-08 完成

**阶段3：最终冲刺（2019-09 ~ 2019-12）**
- 高延迟但重试机制有效
- 2019-09 遇到多次 `ClientConnectorError`，最终通过 5 次重试突破
- **2026-05-26：单层 84/84 全部完成，83 MB**

### 单层数据最终统计
| 年份 | 状态 |
|------|------|
| 2013 | 12/12 |
| 2014 | 12/12 |
| 2015 | 12/12 |
| 2016 | 12/12 |
| 2017 | 12/12 |
| 2018 | 12/12 |
| 2019 | 12/12 |

---

## 气压层 ERA5 数据下载（0/84，探索中）

### ARCO GCS 方案（已废弃）

**尝试1：串行周级下载**
- `level=slice(1000, 1)` → 选中空数据（levels 是升序 1→1000）
- 生成的 18KB 空文件已清理

**尝试2：修正 level 后串行**
- `level=slice(1, 1000)` 修正
- 第一周立即触发 aiohttp TimeoutError

**尝试3：3 线程并行**
- `ThreadPoolExecutor(max_workers=3)`
- 立即触发 GCS 限流

**尝试4：dask 单线程 + 3天窗口**
- `dask.config.set(scheduler='single-threaded')`
- 解决了内部 aiohttp 并发问题
- 每个 3 天窗口 40-50 分钟（极慢）
- 气压层数据量是单层的 37 倍，每约需 7 小时
- 84 月预计 ~23 天，不可接受
- **根本原因**：ARCO 按全球逐小时分块，下载 99.9% 数据被丢弃

### CDS API 方案（当前采用）

- 旧 key `e088c69b` 和 `a2179a74` 已 403
- **新 key**：`8dfcb2f7-****`（完整 key 已从文档移除，请在 CDS 后台轮换）
- 2026-05-26 测试：1天×1层×1变量 28KB，~11s → 可用
- 许可证：已接受 `reanalysis-era5-pressure-levels` 和 `reanalysis-era5-single-levels`

**成本限制实验：**
| 窗口 | 字段数 | 结果 |
|------|--------|------|
| 7天×24h×37层×3变量 | 18,648 | 403 cost limits exceeded |
| 3天×24h×37层×3变量 | 7,992 | 通过 |

**推荐策略：3 天窗口，~1-2min/次，~10次/月，~28h/84月**

### CDS vs ARCO 对比更新

| 维度 | CDS | ARCO GCS |
|------|-----|----------|
| 服务器端裁剪 | ✅ 支持（只返回区域数据） | ❌ 不支持（全球逐小时chunk） |
| 气压层速度 | ~10min/月 | ~7h/月 |
| 限流 | 成本配额（3天窗口通过） | IP级别（隐式超时/取消） |
| 并发 | 不支持（队列系统） | 单线程可工作 |
| 稳定性 | ✅ 较好 | ❌ 频繁限流 |

---

# 会话记录 — 2026-05-27/28/29

## CDS 气压层下载尝试（结论：当前不可用）

### 测试记录

| 日期 | 方式 | 结果 |
|------|------|------|
| 2026-05-27 13:20 | 直连测试(1天×3层×3变量) | 提交成功，卡 accepted 20+ min |
| 2026-05-27 13:53 | 直连 dl_pl_cds.py | DNS 解析失败 (NameResolutionError)，持续重试 |
| 2026-05-27 15:30 | 加代理 `HTTPS_PROXY=127.0.0.1:7897` | SSL EOF 错误 (SSLEOFError)，代理不支持 CDS 长轮询 |
| 2026-05-28 10:34 | 代理重试 | 混合 ProxyError + SSLEOFError + ReadTimeout |
| 2026-05-29 12:47 | 直连测试(1天×1层×1变量) | 提交成功，卡 accepted 30+ min |

### 结论
- **直连**：DNS 偶尔可用（136.156.139.54），请求可提交但队列不处理（accepted 状态卡死）
- **代理**：7897 端口代理不支持 CDS 长轮询（SSL EOF / RemoteDisconnected）
- **CDS 当前不可用于批量下载**

### 新增文件
- `dl_pl_cds.py` — 独立气压层下载脚本，3天窗口，断点续传，CDS 恢复后可直接运行

### 数据现状（2026-05-29）
| 类型 | 文件 | 数量 | 格式 |
|------|------|------|------|
| 单层 | `sl_YYYY_MM.nc` | 84月 | CDS (t2m, d2m, sp, dim=valid_time) |
| 单层(ref) | `sl_2013_01_arco.nc` | 1月 | ARCO (2m_temperature等) |
| 气压层(ref) | `pl_2013_01_arco.nc` | 1月 | ARCO (temperature, geopotential, specific_humidity) |
| 气压层 | — | 0/84 | 待CDS恢复 |

### 策略调整
- **放弃等待 CDS**，先走第3条路线：用现有数据（单层84月 + 气压层1月参考）推进预处理→训练管线
- 2013-01 是唯一同时有单层+气压层的月份，先以此做 POC 验证全流程
- 需适配脚本处理 CDS/ARCO 变量名差异和 specific_humidity→RH 转换
